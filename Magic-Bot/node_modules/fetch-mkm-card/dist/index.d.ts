export declare function fetchMkmCard(name: string): Promise<import("node-fetch").Response>;
export declare namespace fetchMkmCard {
    var init: (keys: {
        appToken: string;
        appSecret: string;
        accessToken: string;
        accessTokenSecret: string;
    }) => void;
}
